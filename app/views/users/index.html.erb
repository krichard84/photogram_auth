<div class="page-header">
  <h1>
    Users
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Users</th>
        <th>Additional Details</th>
      </tr>

      <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        <td>
          <a href="/users/<%= user.id %>" class="btn btn-primary">See Details</a>
        </td>
      </tr>
 
      <% end %>
    </table>
  </div>
</div>